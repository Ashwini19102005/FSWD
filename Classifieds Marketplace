<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Classifieds Marketplace</title>
<style>
/* Reset & General */
* { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Poppins', sans-serif; }
body { background: #0d0d0d; color: #fff; }

/* Header */
header {
    background: linear-gradient(90deg,#6a0dad,#ff1493);
    padding: 1.5rem 2rem;
    text-align: center;
    position: sticky;
    top: 0;
    z-index: 10;
}
header h1 { font-size: 2rem; color: #fff; margin-bottom: 0.5rem; }
header input {
    width: 50%;
    max-width: 400px;
    padding: 0.7rem 1rem;
    border-radius: 25px;
    border: none;
    outline: none;
    font-size: 1rem;
    background: #1a1a1a;
    color: #fff;
}

/* Main Layout */
main { padding: 2rem; display: flex; flex-direction: column; gap: 2.5rem; }

/* Add Product Form */
.add-product {
    background: #1a1a1a;
    padding: 2rem;
    border-radius: 20px;
    box-shadow: 0 0 20px rgba(255,0,255,0.2);
}
.add-product h2 { color: #ff1493; margin-bottom: 1rem; }
.add-product form { display: flex; flex-direction: column; gap: 1rem; }
.add-product input, .add-product textarea {
    padding: 0.9rem;
    border-radius: 12px;
    border: none;
    background: #2b2b2b;
    color: #fff;
}
.add-product button {
    padding: 0.9rem;
    border-radius: 15px;
    border: none;
    font-weight: bold;
    background: linear-gradient(90deg,#6a0dad,#ff1493);
    color: #fff;
    cursor: pointer;
    transition: 0.3s;
}
.add-product button:hover {
    transform: scale(1.05);
    box-shadow: 0 0 15px #ff1493;
}

/* Products Section */
.products-section { background: #1a1a1a; padding: 2rem; border-radius: 20px; box-shadow: 0 0 20px rgba(255,20,147,0.2); }
.products-section h2 { color: #ff1493; margin-bottom: 1rem; }
.products {
    display: grid;
    grid-template-columns: repeat(auto-fit,minmax(280px,1fr));
    gap: 1.5rem;
}

/* Product Card */
.product-card {
    background: #2b2b2b;
    border-radius: 20px;
    overflow: hidden;
    position: relative;
    cursor: pointer;
    transition: 0.3s;
}
.product-card:hover { transform: translateY(-5px); box-shadow: 0 10px 25px rgba(255,20,147,0.5); }
.product-card img { width: 100%; height: 180px; object-fit: cover; }
.product-card .product-info { padding: 1rem; }
.product-card h3 { color: #ff1493; margin-bottom: 0.5rem; }
.product-card .category {
    display: inline-block;
    padding: 0.2rem 0.6rem;
    border-radius: 12px;
    background: #6a0dad;
    font-size: 0.8rem;
    margin-bottom: 0.5rem;
}
.product-card p { color: #ccc; font-size: 0.9rem; margin-bottom: 0.5rem; }
.product-card .price { font-weight: bold; color: #ff1493; font-size: 1.1rem; }

/* Footer */
footer { text-align: center; padding: 1rem; background: #1a1a1a; border-top: 1px solid #333; }

/* Responsive */
@media(max-width:600px){ header input{width:80%;} }
</style>
</head>
<body>

<header>
    <h1>✨ Classifieds Marketplace</h1>
    <input type="text" id="searchInput" placeholder="Search products...">
</header>

<main>
    <section class="add-product">
        <h2>Add a Product</h2>
        <form id="productForm">
            <input type="text" id="productName" placeholder="Product Name" required>
            <input type="text" id="productCategory" placeholder="Category" required>
            <input type="number" id="productPrice" placeholder="Price" required>
            <input type="url" id="productImage" placeholder="Image URL" required>
            <textarea id="productDescription" placeholder="Description" required></textarea>
            <button type="submit">Add Product</button>
        </form>
    </section>

    <section class="products-section">
        <h2>Available Products</h2>
        <div class="products" id="productsContainer"></div>
    </section>
</main>

<footer>
    <p>© 2025 Classifieds Marketplace. All rights reserved.</p>
</footer>

<script>
const productForm = document.getElementById('productForm');
const productsContainer = document.getElementById('productsContainer');
const searchInput = document.getElementById('searchInput');

let products = [];

function renderProducts(filter = '') {
    productsContainer.innerHTML = '';
    const filtered = products.filter(p => p.name.toLowerCase().includes(filter.toLowerCase()));
    filtered.forEach(p => {
        const card = document.createElement('div');
        card.className = 'product-card';
        card.innerHTML = `
            <img src="${p.image}" alt="${p.name}">
            <div class="product-info">
                <h3>${p.name}</h3>
                <span class="category">${p.category}</span>
                <p>${p.description}</p>
                <p class="price">₹${p.price}</p>
            </div>
        `;
        productsContainer.appendChild(card);
    });
}

productForm.addEventListener('submit', e => {
    e.preventDefault();
    const newProduct = {
        name: document.getElementById('productName').value,
        category: document.getElementById('productCategory').value,
        price: document.getElementById('productPrice').value,
        image: document.getElementById('productImage').value,
        description: document.getElementById('productDescription').value
    };
    products.push(newProduct);
    productForm.reset();
    renderProducts(searchInput.value);
});

searchInput.addEventListener('input', () => renderProducts(searchInput.value));
</script>

</body>
</html>
